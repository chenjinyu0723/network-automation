# Do not put real keys or device credentials in this file.
# Configure providers in the Settings page. Values are stored locally as secret references.
APP_DATA_DIR=./data
APP_HOST=127.0.0.1
APP_PORT=8000
