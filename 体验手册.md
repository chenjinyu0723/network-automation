# AI Agent 工业交换机自动配置：体验手册

> **2026-08-17 更新说明**：本手册中早期“只支持单 VLAN Access”“缺证据即 blocked”“前端不能编辑命令”的描述已被替换。当前支持双/多 VLAN 的跨交换机互通草案：PC 链路生成 Access、交换机链路生成 Trunk、三层核心生成 VLANIF。手册证据、拓扑推断和 LLM 审查显示为审阅信息，不会代替用户的下发决定；规划页可直接编辑命令并逐台确认。用户明确标记的保护端口仍不会自动生成或写入。

> 适用版本：当前工作区 `0.1.0`。本文以项目中已导入的华为 S1700/S5700/S6700 CHM 手册和本地 eNSP 实验环境为例。所有密码、API Key 均请在界面中自行输入，本文不记录真实凭据。

## 1. 先了解本次体验范围

应用是本地单用户 Web 工作台，核心路径为：**注入手册 → 绘制拓扑 → 为任务选择手册 → 第一步生成/编辑/确认配置思路 → 第二步生成结合手册证据的命令草案 → 逐台查看/确认 → 单台下发、验证并自动保存 →（可选）PC SSH ping**。

当前首个可执行能力是“创建 VLAN，并把已映射的交换机端口配置为 Access 加入该 VLAN”。系统的重点是生成可审阅命令草案：手册缺项、拓扑推断或 LLM 审查结果会显示为提示，用户可在规划页查看/编辑命令，再自行决定是否逐台下发。用户明确标记的保护端口仍不会自动生成或写入。

本手册分为三条路径：

1. **A. 无风险走查**：安装、查看设置、手册、拓扑、检索和规划；不连接设备。
2. **B. 只读设备识别**：仅发送 `display version`，记录现场型号和版本供审计；不进入配置视图。
3. **C. 实验设备单台下发**：会修改设备运行配置，仅适用于已获授权的隔离实验环境。

当前已知限制也需要先说明：

- 规划链路已接入 LangGraph：先取确定性 VLAN 基线，再可选调用设置页的 LLM 精炼检索词/说明，随后执行命令名 + FTS5 + 可选 CPU Embedding 混合检索。模型可用时，LLM 生成受约束的 `CommandPlan`（仅能引用检索到的手册命令和拓扑端口），确定性编译器再生成 CLI；模型不调用工具或 SSH。模型服务未配置或输出不合格时会自动回退到确定性 VLAN 演练。
- 手册管理的 Web 界面当前上传时固定标记品牌为 `Huawei`，未暴露“品牌/版本”输入框；后端管道支持 CHM、HTML、文本/Markdown、PDF，但多品牌元数据录入需要后续补 UI。
- 当前“配置规划”页只能审核生成的命令，尚未提供图形化命令编辑器；后端 API 支持受 revision 保护的命令覆盖，界面尚未接通。
- 当前“下发与结果”页直接从下拉列表选择配置任务与设备计划，不需要复制任务 ID 或设备计划 ID。
- 端口文本会原样用于 CLI：填写 `GE0/0/1` 就生成 `interface GE0/0/1`，填写 `GigabitEthernet0/0/1` 同理。但端口去重、保护端口和回显验证将两者按同一物理口比较；因此把 `GigabitEthernet0/0/2` 保护起来后，用 `GE0/0/2` 连线也会被阻断。请选择目标设备手册/CLI 确认可接受的写法，且绝不将受保护端口加入拓扑。
- 首期 VLAN Access 只会配置交换机直接连到 `PC` 图标的端口。交换机间上联、云连接和对端不明确的连线不会出现在命令中；即便通过命令覆盖尝试加入，执行前也会拒绝。
- PC ping 页面当前默认并固定使用 Linux ping 形式；Windows 选项虽在后端支持，前端选择器尚未提供。没有 PC SSH 权限时跳过该项即可。

## 2. 安装前检查

在 PowerShell 中进入项目根目录：

```powershell
cd D:\network-automation
```

建议环境如下。

| 组件 | 用途 | 检查命令 |
| --- | --- | --- |
| Windows 10/11 x64 | 本地运行 | `Get-ComputerInfo` |
| Python 3.11 或 3.12、uv | FastAPI 后端和工具依赖 | `uv --version` |
| Node.js 22、pnpm 11.9 | React 前端 | `node --version`、`pnpm --version` |
| 7-Zip | 解包 CHM | `7z` |
| 浏览器 | 访问本机界面 | Edge / Chrome |

如果 `uv` 或 7-Zip 未安装，可用常用的 Windows 包管理器安装；安装完成后请重新打开 PowerShell，使 PATH 生效。

```powershell
winget install --id astral-sh.uv
winget install --id 7zip.7zip
```

项目已固定 pnpm 版本为 `11.9.0`。如机器没有 pnpm，可先启用 Corepack：

```powershell
corepack enable
corepack prepare pnpm@11.9.0 --activate
```

## 3. 第一次启动

### 3.1 安装后端依赖

在项目根目录执行：

```powershell
uv sync --extra dev
```

这会创建或更新 `.venv`，不会连接网络设备。首次运行后端会自动创建本地数据目录和 SQLite 数据库：

```text
data/
├─ network_automation.db       # SQLite 数据库
├─ manuals/original/           # 导入的手册原件
├─ manuals/extracted/          # CHM 解包及抽取中间产物
├─ exports/                    # 预留的导出目录
└─ logs/                       # 导入、Embedding Worker 日志
```

`data/` 不应提交到 Git；其中可能包含手册内容和运行记录。模型 API Key 不写入 SQLite，而是交由本机系统凭据存储保存引用。

### 3.2 安装前端依赖

仍在**项目根目录**执行：

```powershell
pnpm install --frozen-lockfile
```

项目已在 `pnpm-workspace.yaml` 显式批准 `esbuild` 构建脚本。若看到 `esbuild` 的 ignored-build 提示，请不要在 `apps/web` 子目录运行安装，也不要反复运行 `pnpm approve-builds`；请回到根目录执行上面的命令。

不要使用：

```powershell
pnpm --dir apps/web install
```

该命令会让 pnpm 11 将工作区前端误按独立项目处理，可能出现“已批准 esbuild 被忽略”的误报。

### 3.3 分别启动后端和前端

打开两个 PowerShell 窗口。

窗口 A（后端）：

```powershell
cd D:\network-automation
uv run uvicorn app.main:app --reload --app-dir apps/api
```

看到类似 `Uvicorn running on http://127.0.0.1:8000` 即表示启动成功。可访问 [健康检查](http://127.0.0.1:8000/api/health)，预期响应为：

```json
{"status":"ok"}
```

窗口 B（前端）：

```powershell
cd D:\network-automation
pnpm --filter network-automation-web dev
```

浏览器打开 [http://127.0.0.1:5173](http://127.0.0.1:5173)。左侧应看到五个页面：**拓扑编辑、手册管理、配置规划、下发与结果、设置**。

可选的生产构建检查：

```powershell
pnpm --filter network-automation-web run build
```

构建可能提示前端 bundle 大于 500 kB；这是性能提示，不影响当前功能运行。

## 4. 路径 A：无风险完整走查

### 4.1 设置页：了解模型配置和密钥边界

进入“设置”。页面包含：

- **LLM**：Base URL、API Key、模型名、Temperature、推理（thinking）策略。
- **Embedding**：Base URL、API Key、模型名、向量维度。
- **服务状态**：只检查本机后端 `/api/health` 是否可达。

所有模型端点都按 OpenAI 兼容接口调用；传输层按项目约束固定为 `httpx verify=False`，因此只能填入可信的内网/私有端点。

体验建议：

1. 如果暂时没有模型服务，不填也可以继续；本次 VLAN 演练会使用命令名匹配 + SQLite FTS5 检索和确定性规划。
2. 若有服务，填写占位之外的真实地址、模型名和 Key 后点击“保存本地设置”。
3. 保存后 API Key 输入框会清空；重新打开页面只会显示“已配置”，不会回显密钥。
4. 当前版本没有“调用 LLM/Embedding 测试”按钮；LLM 是否工作可在生成计划后的 Intent 中查看 `llm.status`，Embedding 是否真正可用以手册页启动索引任务后的状态及检索结果的 `embedding_cpu` 标签为准。

#### thinking 策略怎么选

- **自适应（推荐）**：需求理解等推理型节点开启 thinking，静态校验、端口保护、执行和保存不调用 LLM。
- **始终开启**：所有实际 LLM 请求开启，适合调试模型推理质量，但延迟和消耗更高。
- **始终关闭**：所有请求关闭 thinking，适合确认兼容端点是否支持基础 Chat Completions。

开启策略时客户端会发送 `chat_template_kwargs.enable_thinking=true` 和 `thinking.type=enabled`。如果端点明确不支持这些可选字段，系统自动重试一次关闭 thinking；计划审计会标记降级，不会把兼容性错误伪装成成功。

### 4.2 手册管理：查看已注入的华为 CHM 样例

进入“手册管理”。项目当前已有一份样例：

`S1700, S5700, S6700 V600R025C00 命令参考.chm`

预期能在表格看到（允许因重新导入而略有不同）：

| 指标 | 样例结果 |
| --- | ---: |
| 页面 | 8,914 |
| 命令 | 8,714 |
| 失败页 | 0 |

这一步可以验证：CHM 已由本机 7-Zip 解包，HTML TOC、命令页、命令格式、视图、参数、约束和示例已被抽取到 SQLite；手册内容没有上传到外部服务。

#### 体验一次手册导入（可选）

如果想从零体验导入，请把样例 CHM 拖入上传区域，或导入一份测试用 `.chm`、`.html`、`.htm`、`.txt`、`.md`、`.pdf` 文件。导入相同文件时系统按 SHA-256 去重，可能直接返回已有任务，这是预期行为。

导入任务会显示：

- 阶段与状态：`queued`、`running`、`completed`、`completed_with_issues` 或 `failed`；
- 已处理页数 / 总页数；
- 命令数和失败页数；
- 失败时的“从断点重试”。

对于 CHM，失败时优先检查：7-Zip 是否可在 PATH 中调用、`data/logs/` 中对应 Worker 日志、以及 CHM 是否含标准 HTML 页面。不要依赖 Windows `hh.exe -decompile`，部分 CHM 会静默产生空目录。

#### 可选：构建 Embedding 索引

若设置页已配置并保存 Embedding 服务，点击某份已完成手册行内的“构建 Embedding”。Worker 会分批把命令知识写入 SQLite BLOB，并在 CPU 上做余弦检索；无需 GPU 或云向量库。

不配置 Embedding 或索引尚未完成时，系统继续使用命令名匹配和 SQLite FTS5，当前 VLAN 演练不受影响。索引完成后，规划页手册证据的检索标签会增加 `embedding_cpu`（同时命中时）。

### 4.3 选择任务手册并检索真实命令

不需要在拓扑中填写型号，也没有型号库审核步骤。保存拓扑后，在“配置规划”选择一份状态为 `completed` 或 `completed_with_issues` 的手册；它就是本次任务的唯一命令上下文。只读 `display version` 结果会展示在计划中供审计，但不会阻断规划、审批或下发。

进入“配置规划”。在顶部检索框依次输入并查询：

```text
vlan batch
port link-type
port default vlan
```

每条结果应显示命令名、命令格式和源文件路径。这一步是“规划意图 ↔ 手册命令”配对的可观察证据：计划不会仅凭模型文本生成命令，而是要求找到这三类手册记录。

对于后续 VLAN Access 演练，缺少其中任一命令，或命令格式不能证明 `port default vlan` 可在接口视图使用，计划都会变为 `blocked`。

## 5. 路径 B：只读识别实验交换机（不改配置）

> 仅在你明确授权的实验交换机上执行。本步骤只会执行 `display version`，不进入 `system-view`、不查询或修改端口、不会保存配置。

以现有 eNSP 环境为例，在“拓扑编辑”页先添加一个交换机节点：

1. 点击“交换机”，选中新增的 `SW1`。
2. 在右侧“设备属性”中填写 SSH IP、端口和用户名；密码不在此页保存，而是在弹出的只读连接窗口临时输入。
3. 在“受保护端口”填写设备文档/CLI 使用的格式，例如 `GigabitEthernet0/0/2`；系统也会将等价的 `GE0/0/2` 识别为该保护口。
4. 点击“仅查询型号/版本”。确认弹窗内容明确写着只执行 `display version` 后，再临时输入 SSH 密码并确认。

成功后，右侧只读字段应自动出现类似：

```text
只读识别型号：S5700-28C-HI
只读识别版本：V200R001C00
```

这说明已取得现场型号和版本的审计信息。它不会因为与所选手册标题中的系列或版本不同而阻断本期手册驱动流程。

若连接失败，系统会报“只读 SSH 探测失败”；此时没有发送配置命令。先在 eNSP 确认管理地址、SSH 服务、用户权限与本机网络可达性，不要改写脚本绕开只读白名单。

## 6. 创建演练拓扑

继续在“拓扑编辑”页完成一个最小拓扑。该拓扑只把一个明确的接入口纳入配置范围。

### 6.1 添加节点和连线

1. 保留 `SW1`，再点击“PC 终端”得到 `PC1`。
2. 点击“连线（绳）”，再依次点击 `SW1` 和 `PC1`，系统会生成一条直线；设备卡片上不显示端点圆点。
3. 点击这条直线，右侧会显示“链路接口”面板，分别填写两端接口名并点击“保存接口名”：

| 设备 | 端口填写值 |
| --- | --- |
| PC1 | `Ethernet0/0/1` |
| SW1 | `GE0/0/1` 或 `GigabitEthernet0/0/1`（按目标设备 CLI 选择） |

面板会明确显示每个设备对应的输入框，不需要判断源/目标方向。你填写的 `GE0/0/1` 或 `GigabitEthernet0/0/1` 会被原样用于计划和设备验证；两种写法只在安全比较时视为同一端口。

4. **不要创建任何指向 `GigabitEthernet0/0/2` 或 `GE0/0/2` 的连线。** 该端口不属于本次配置范围。
5. SW1 的“受保护端口”保留设备 CLI 对应的写法，例如 `GigabitEthernet0/0/2`。

PC 的 IP、掩码前缀、网关可以填写为演练资料；PC 默认没有 SSH 字段。交换机的 IP、掩码前缀、网关和 SSH 字段全部是可选的，未填写时保持为空；真正连接参数以 SSH IP/端口/用户名为准。

### 6.2 保存不可变 revision

在左侧填写拓扑名称后点击“保存拓扑”。首次保存会创建 revision 1，后续继续编辑同一拓扑并保存会追加 revision；完整的拓扑 ID 和最近 revision 会保存在本地数据库及浏览器记录中。刷新页面时会自动恢复最近拓扑，也可以从左侧“已保存拓扑”下拉框打开历史拓扑的最新 revision。

保存后如果修改拓扑或端口，应重新保存并重新创建任务。计划绑定的是保存时的拓扑快照，而不是之后可能被改动的画布。

## 7. 从需求到受限计划

进入“配置规划”，在“创建配置任务”区域按以下填写：

- 拓扑 revision ID：通常已自动带出；为空时说明换了浏览器或清过站点数据，需要重新保存拓扑。
- 手册：选择状态为 `completed` 或 `completed_with_issues` 的华为 CHM。
- 需求：

```text
创建 VLAN 10，并将交换机接入端口配置为 Access 加入 VLAN 10。
```

点击“生成受限规划”。预期得到一个 `needs_review` 的任务，以及 SW1 的设备计划，内容包括：

1. 显示所选手册上下文；若已做过只读查询，也会显示现场型号 `S5700-28C-HI` 和版本，但它们只用于审计。
2. 手册证据至少包含 `vlan batch`、`port link-type`、`port default vlan`。
3. Intent 中 `llm.status` 显示 `accepted`、`fallback` 或 `disabled`：三者均可继续，只有前者表示模型补充的检索词被采纳。若已配置可用 LLM，`llm_command_plan.status=accepted` 表示模型还完成了 CommandPlan；它只能引用已检索的手册命令、VLAN 参数和拓扑端口，随后由确定性编译器生成下方 CLI。模型不可用时，该字段为 `disabled/fallback`，系统回退既有模板。
4. 受审核的命令大致为（端口保持为你拓扑填写的文本；以下仅以完整写法举例）：

```text
system-view
vlan batch 10
interface GigabitEthernet0/0/1
port link-type access
port default vlan 10
quit
return
```

5. 静态检查项包括 VLAN 范围、先建 VLAN 后引用、端口来自拓扑而非猜测、明确设置 Access。
6. 页面若出现“LLM CommandPlan 已由确定性编译器生成”标签，说明当前命令通过了命令 ID、语法索引、参数与端口来源校验；这不是自动下发授权，仍必须逐台确认。
7. 设备侧验证命令为：

```text
display vlan 10
display port vlan GigabitEthernet0/0/1
```

任何包含受保护端口、没有端口映射、所选手册未完成抽取、或手册证据不足的情况，都会在设备计划中显示提示；命令草案不会因为 LLM 审查或证据提示被自动清空。请以规划页展示的命令为准，人工判断是否需要编辑或下发。

## 8. 审核本设备命令集

在设备计划卡片中逐项核对：

- 设备名称是否为预期的 SW1；
- 所选手册是否与目标设备及需求匹配；现场型号/版本（如有）仅作审计记录；
- 端口是否只出现你在拓扑填写的 `GE0/0/1` 或 `GigabitEthernet0/0/1`，且没有受保护端口的任一等价写法；
- 命令中没有 `save`、`reboot`、`reset`、`delete`、`clear` 等维护命令；
- 验证目标只包含 VLAN 10 与该授权端口。

确认无误后点击“确认本设备命令集”。

审批只确认这一台设备的这一版命令，**不等于已经下发**。若以后后端收到命令覆盖，审批 revision 会更新；旧的审批不能用于写入。

## 9. 路径 C：实验设备单台下发

> 警告：本节会实际写入交换机，并且设备验证通过后自动执行 `save`。请只在隔离实验环境、明确授权的设备与端口上操作。不要把本教程当作生产变更流程。

### 9.1 下发前最终检查

在点击“确认仅下发本设备”前，逐条确认：

- 你已在第 8 节批准当前设备计划。
- 计划中仅有 `GigabitEthernet0/0/1`。
- 拓扑中没有 `GigabitEthernet0/0/2` 的连接。
- 保护端口填的是完整字符串 `GigabitEthernet0/0/2`。
- 设备管理地址、端口和用户名与冻结拓扑一致。
- 你接受验证成功后系统自动 `save`。

系统写入前还会记录 `display version` 的现场回显，并检查手册上下文、审批 revision、冻结 SSH 地址/端口、受保护端口、前序设备状态和静态校验。任何一项失败，执行状态为 `preflight_blocked`，不会建立写会话或发送配置。

### 9.2 执行一台设备

进入“下发与结果”，填写：

| 字段 | 内容 |
| --- | --- |
| 配置任务 | 从下拉列表选择已生成的任务 |
| 设备计划 | 从下拉列表选择已审批的 SW1 计划 |
| SSH IP / 端口 / 用户 | 必须与拓扑冻结值一致 |
| 本次 SSH 密码 | 仅用于本次连接，不保存 |

点击红色按钮“确认仅下发本设备”。一次请求只会针对一个设备计划，页面没有批量下发按钮。

预期顺序如下：

1. 重新执行 `display version`，将现场型号和版本回显记录到执行审计；
2. 逐条发送审核过的配置命令；任一设备回显命令错误即停止；
3. 只执行 `display vlan 10` 和 `display port vlan GigabitEthernet0/0/1`；
4. 验证通过后，单独执行交互式 `save`，记录 Y/N 与默认文件名交互的回显；
5. 最终状态显示为 `completed`，并展示每条命令的阶段、命令、设备回显、成功状态和 `save` JSON。

如验证失败，状态应为 `validation_failed`，并且 `save.attempted` 必须是 `false` 或空对象；这时不要手动重复点击下发，应先分析执行记录和设备实际状态。

### 9.3 可选：PC SSH ping 验收

仅在交换机执行状态为 `completed` 后，页面才展示“PC SSH ping 验收”。填写受授权 PC 的 SSH 地址、端口、用户名、密码和目标 IPv4 后，系统仅允许执行白名单 ping：

```text
Linux:   ping -c 4 -W 2 <目标 IPv4>
Windows: ping -n 4 -w 2000 <目标 IPv4>
```

PC 密码同样不保存。eNSP 中只有 DHCP 功能的 PC 不一定提供 SSH；如果没有明确的 PC SSH 地址、操作系统和凭据，请跳过本项，改为在 PC 控制台人工 ping 并把结果记录在变更单中。

## 10. 结果判读与排错

| 现象 | 常见原因 | 处理方式 |
| --- | --- | --- |
| 前端打不开 | Vite 未启动或端口被占用 | 在窗口 B 重启前端，确认 `http://127.0.0.1:5173` |
| 后端健康检查失败 | `uvicorn` 未启动、Python 环境未同步 | 在窗口 A 查看错误，重新执行 `uv sync --extra dev` |
| CHM 导入失败 | 未安装/找不到 7-Zip、CHM 异常 | 检查 `7z`、`data/logs/`，点击“从断点重试” |
| 计划有“规划提示” | 所选手册未完成、端口映射或手册证据不足 | 查看设备计划提示并人工修订/判断命令；不通过手改数据库绕过用户界面 |
| 审批失败 | 静态验证不通过或审批 revision 已过期 | 重新创建/审阅计划；当前 UI 不支持直接编辑命令 |
| `preflight_blocked` | SSH 地址不一致、未审批、计划含保护端口等 | 对照冻结拓扑与预检错误；不会下发 |
| `command_failed` | 设备拒绝某条命令 | 查看该命令回显，停止并人工处理，不自动回滚 |
| `validation_failed` | VLAN 或目标端口未满足预期 | 系统不会 `save`；比较验证回显与期望状态 |
| `save` 失败 | VRP 交互提示变化或设备保存失败 | 配置已验证但保存不确定；人工检查设备保存状态，不盲目重发 |
| `esbuild` ignored-build 报错 | 在子目录安装触发 pnpm 工作区误判 | 回到根目录执行 `pnpm install --frozen-lockfile` |

## 11. 一次合格体验的验收清单

完成以下项目即可认为当前版本的体验走通：

- [ ] 后端 `/api/health` 返回 `ok`，前端能打开五个页面。
- [ ] 看到了 CHM 手册的状态、页数、命令数和失败页数。
- [ ] 在规划页选择了正确且已完成抽取的手册，并理解这是本次任务的命令上下文。
- [ ] 在规划页检索到了 `vlan batch`、`port link-type`、`port default vlan` 的手册证据。
- [ ] 保存了一个包含 SW1、PC1 和一个明确授权接入口（`GE0/0/1` 或 `GigabitEthernet0/0/1`）的拓扑 revision。
- [ ] 对真实设备仅在必要时执行了 `display version`，并将型号/版本作为审计记录；没有查询或触碰保护端口。
- [ ] 生成的计划保留你填写的端口文本，且未包含受保护端口的 `GE` 或 `GigabitEthernet` 任一等价写法。
- [ ] 已理解“审批”和“下发”是两个独立动作。
- [ ] 如执行实验下发：设备验证通过、执行记录完整、`save.success=true`；如未执行，已止步于审批，未产生设备写入。
- [ ] 如具备 PC SSH：仅执行白名单 ping；如不具备，已跳过且不把它误记为自动验收。

## 12. 建议的下一步

在把系统用于更多实验前，建议优先完成以下改进：

1. 在已审批设备计划上增加到“下发与结果”页的直达入口，减少跨页查找。
2. 增加命令编辑、编辑后的再次静态校验与审批 revision 更新界面。
3. 完成多品牌/多版本手册上传元数据表单、命令证据查看，以及 Embedding 索引进度显示。
4. 为更多厂家/接口类型补充经过手册验证的端口别名规则，未知接口仍保持精确比较。
5. 为新增网络功能分别定义 Intent Schema、LLM 一致性约束、手册证据门和确定性 renderer。
