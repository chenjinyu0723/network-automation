# AI Agent 工业交换机自动配置：工作流与算法流程报告

> 这份报告解释当前系统怎样从手册、拓扑和需求，走到逐台审批、下发、验证和保存。内容以当前代码的真实行为为准；已经实现、仅做基础设施、以及尚未实现的部分会明确区分。实际操作可配合 [体验手册](D:\network-automation\体验手册.md)。

## 1. 核心思想：不是让模型直接下命令

系统不是“把需求交给大模型，再把它回答的 CLI 直接发送到交换机”。它是一条受证据和安全门约束的变更流水线：

~~~text
手册 → 命令证据、型号候选、全文索引
拓扑 → 被允许操作的设备和端口范围
需求 → 结构化意图 Intent
意图 + 证据 + 拓扑 → 候选命令 + 验证命令
人工审批 → 某一台设备、某一版命令的写权限
设备回显 → 验证成功后才 save；失败则停止
~~~

因此，只要发生下列情况，系统应停止而不是猜测：

- 手册中找不到必需命令；
- 端口没有被拓扑明确映射；
- 型号不在当前手册覆盖的系列中；
- 映射端口是受保护端口；
- 用户没有审批当前命令 revision；
- 设备侧验证没有满足预期。

当前首个可写入的受限能力是：

~~~text
创建 VLAN，并把拓扑中明确映射的交换机端口设为 Access、加入指定 VLAN。
~~~

其它自然语言需求会被标记为 unclassified，不能产生写命令。

## 2. 总体架构与数据流

~~~mermaid
flowchart LR
  U["用户 / 本机浏览器"] --> FE["React + Ant Design + React Flow"]
  FE --> API["FastAPI"]
  API --> DB[("SQLite + FTS5")]
  API --> W1["本地手册导入 Worker"]
  API --> W2["可选 Embedding Worker"]
  W1 --> FS["data/manuals 原件与解包内容"]
  W1 --> DB
  W2 --> DB
  API --> LG["LangGraph 规划状态机"]
  LG --> DB
  API --> NM["Netmiko"]
  NM --> SW["授权的单台交换机"]
  API --> PC["可选：SSH 到授权 PC ping"]
~~~

### 2.1 本地数据与凭据边界

| 内容 | 保存位置 | 说明 |
| --- | --- | --- |
| 业务数据 | data/network_automation.db | SQLite：手册、命令、型号、拓扑、任务、审批、执行审计 |
| 全文索引 | SQLite FTS5 的 command_search | 命令名、命令格式和手册正文的本地检索 |
| 手册原件 | data/manuals/original | SHA-256 命名，用于去重和追溯 |
| CHM 解包结果 | data/manuals/extracted/手册 ID | 7-Zip 解出的 HTML、目录和资源 |
| Worker 日志 | data/logs | 导入或向量索引的故障排查 |
| LLM/Embedding Key | Windows 系统凭据存储 keyring | SQLite 只保存密钥引用 |
| 交换机/PC SSH 密码 | 请求和连接的内存 | 不写 SQLite、不写执行日志、不持久化到浏览器 |

SQLite 启用了 WAL、外键和 15 秒 busy timeout，使导入 Worker 与 Web API 可以短暂并行而不易出现数据库锁错误。

### 2.2 三类输入分别解决什么问题

| 输入 | 谁提供 | 系统把它当作什么 | 它不能替代什么 |
| --- | --- | --- | --- |
| 手册 | 用户导入 | 命令语法、约束、适用性和型号候选的证据源 | 现场端口和现场运行状态 |
| 拓扑 | 用户手绘、填写 | 可配置设备、连线端口、保护端口、管理连接冻结信息 | 命令对型号的适用性 |
| 需求 | 用户文字 | 要达成的业务意图 | 可直接执行的 CLI |
| 设备只读回显 | 按需 SSH 查询 | 现场型号和版本 | 手册证据与人工审批 |

## 3. 整体状态机

~~~mermaid
flowchart TD
  A["上传手册"] --> B["格式识别、SHA-256 去重"]
  B --> C["本地 Worker 抽取文档、命令、型号"]
  C --> D{"手册完成？"}
  D -- "否" --> D1["失败记录 / 断点重试"]
  D -- "是" --> E["用户审核型号映射"]
  E --> F["手绘拓扑并保存 revision"]
  F --> G["按需只读识别设备型号"]
  G --> H["创建配置任务"]
  H --> I["型号/系列判定 + 确定性 Intent 基线"]
  I --> I1["LLM 受 Schema 约束的 Intent/检索词精炼（可降级）"]
  I1 --> J["混合检索定位手册命令证据"]
  J --> K{"证据、端口、型号门禁都通过？"}
  K -- "否" --> K1["blocked：不生成写命令"]
  K -- "是" --> L["渲染候选命令和验证命令"]
  L --> M["LangGraph 状态检查"]
  M --> N["needs_review"]
  N --> O["人工逐台审核与批准"]
  O --> P["执行前二次预检"]
  P --> Q{"预检通过？"}
  Q -- "否" --> Q1["preflight_blocked：不建立写会话"]
  Q -- "是" --> R["再次执行 display version"]
  R --> S{"仍归属审批系列？"}
  S -- "否" --> S1["failed：未发送配置"]
  S -- "是" --> T["逐条下发本设备配置"]
  T --> U{"设备拒绝命令？"}
  U -- "是" --> U1["command_failed：停止、不 save"]
  U -- "否" --> V["验证命令 + 期望状态断言"]
  V --> W{"验证通过？"}
  W -- "否" --> W1["validation_failed：停止、不 save"]
  W -- "是" --> X["交互式 save"]
  X --> Y{"收到明确成功回显？"}
  Y -- "否" --> Y1["failed：人工确认保存状态"]
  Y -- "是" --> Z["completed"]
  Z --> AA["可选：授权 PC SSH ping"]
~~~

## 4. 手册注入：从多格式文件到知识库

### 4.1 上传、识别格式、去重

前端向 POST /api/manuals/upload 发送文件。后端按扩展名判断格式，并计算整个文件的 SHA-256：

~~~text
.chm              → chm
.html / .htm      → html
.txt / .md        → text
.pdf              → pdf
其它              → unknown，导入会失败
~~~

若数据库已有相同 SHA-256，系统直接返回已有手册和最近导入任务，不重复抽取。否则保存原件，创建 Manual 和 ImportJob，并启动独立本地 Worker。单文件大小上限是 1 GiB。

### 4.2 CHM 为什么先解包

CHM 是编译后的 HTML Help 容器，不能像普通文本直接解析。当前路径：

~~~text
CHM
  └─ 7z x <file>.chm -o<extracted-dir> -y
       ├─ *.hhc    目录 TOC
       └─ *.html   主题页、命令页
~~~

导入器按 PATH、Program Files、Program Files (x86) 查找 7-Zip。解包后若没有 HTML 页面，直接失败，避免“空知识库却显示成功”。

Windows 的 hh.exe decompile 没有被当作依赖，因为它对部分 CHM 会静默产生空目录。

### 4.3 华为 CHM/HTML 的页面抽取算法

每个 HTML 页依次经历：

1. 解码尝试：声明 charset → GBK → UTF-8 → GB18030 → Latin-1 → 替换字符兜底；
2. 解析 HHC 的 sitemap，得到页面 TOC 路径；
3. 使用 BeautifulSoup 解析 DOM；
4. 同时有 clifunc 和 cliformat 才认定为“命令页”；普通章节也保存，但不产生 Command；
5. 抽取命令名、功能、命令格式、视图、参数表、前置条件、约束、示例、源路径和 TOC；
6. 从页面正文识别型号 token 和“该命令……产品上支持”类显式适用性证据；
7. 每 100 页提交一次 SQLite 与 FTS5；单页异常累计到 issue_count，不让整本手册失败。

华为命令格式不是对整个页面做简单文本提取。解析器优先读取 cliformatbody 下的段落，保留“一条 CLI 语法一行”。例如样例手册中 vlan batch 实际得到：

~~~text
vlan batch { vlan-id1 [ to vlan-id2 ] } &<1-10>
undo vlan batch { vlan-id1 [ to vlan-id2 ] } &<1-10>
~~~

这让后续流程能验证“找到的是命令语法”，而不是只匹配到相关说明文字。

### 4.4 文本、PDF、一般 HTML 的真实能力边界

文本/Markdown 当前会形成一个知识文档；PDF 用 PyMuPDF 按页提取文本。它们会尝试识别型号 token，但当前不会可靠抽取命令页、命令格式、参数和示例，通常 command_count 为 0。

因此它们目前可导入、可保存、可作为后续适配基础，但不能像华为 CHM 一样生成可执行 VLAN 计划。一般 HTML 若缺少相应的命令页 DOM 结构，也只会作为普通文档入库。

所谓“通用管道”是指上传、去重、Worker、文档存储、型号证据、全文索引的框架通用；每个品牌仍需增加自己的 HTML/PDF 命令页适配器。

### 4.5 断点恢复

导入由独立本地子进程执行，不直接挂在 FastAPI 请求上：

- ImportJob 保存状态、阶段、进度、PID、心跳和详情；
- 已写入 KnowledgeDocument 的 source_path 是断点；
- 重试只处理剩余页面，不删除已完成页面；
- 启动应用时，发现 running 任务 PID 已不存在，会标记 interrupted/failed；
- 单页失败只增加 issue_count，最终可以是 completed_with_issues。

这样一个 8,000 多页 CHM 不会因为浏览器刷新或后端热重载而从头处理。

## 5. 型号库：型号、系列与手册命令的关系

### 5.1 数据结构

型号树是：

~~~text
series（系列）
  └─ family（产品族）
       └─ sku（具体 SKU）
~~~

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| device_models | canonical_name、level、parent_id、review_status、confidence | 型号树 |
| model_aliases | model_id、alias | 手册/设备回显的别名 |
| model_evidence | document_id、evidence_text、confidence | 映射来源和可信度 |
| commands | 语法、视图、约束、来源路径 | 命令知识 |
| command_applicability | command_id、model_id、is_supported | 明确的命令适用性 |

### 5.2 当前华为自动建模规则

当前华为 token 正则覆盖 S17xx、S57xx、S67xx、S77xx、S127xx 一类名称。这里的“子系列”只用于自动建候选和目录未命中时的有限兜底，绝不替代手册覆盖门禁。每发现一个型号 token：

1. 前缀 S17/S57/S67/S77/S127 分别归入 S1700/S5700/S6700/S7700/S12700；
2. 系列以置信度 100 自动发布；
3. 产品族默认为 candidate，置信度约 75；
4. SKU 名称会推导 family + sku，推导出的 family 置信度更低；
5. 名称同时写入 alias，页面原文写入 model_evidence；
6. 命令页出现明确支持产品语句时，才生成显式 CommandApplicability；否则保持 inherit_with_exceptions。

人工可以修正父级、加别名、发布或拒绝候选。这样“能从文本找到型号”和“经过审核、可用于拓扑”是两件不同的事。

### 5.3 已实现的型号树系列兼容判定

规划时设备型号来源优先级：

~~~text
只读 SSH 得到的 detected_model
  > 拓扑预留 model_name
  > 用户从型号库选择的名称
~~~

规划与执行前的型号解析共用 `resolve_series_for_model`，严格按下列顺序执行：

~~~text
1. 在当前品牌的型号库中，以忽略大小写、忽略空白的精确方式匹配 canonical_name 或 model_aliases.alias；不做模糊型号匹配。
2. 从命中的型号节点沿 parent_id 向上遍历，直到 level = series。
3. 只有该系列属于当前手册已抽取的覆盖集合时，才返回兼容结果。
4. 命中节点或任意父级为 rejected、父链成环、或多个候选分别指向不同的已覆盖系列时，返回无法解析并阻断。
5. 目录没有可用结果时，按有限的华为 S 系列子系列规则兜底：S17/S57/S67/S77/S127 分别导向 S1700/S5700/S6700/S7700/S12700；例如 S5700-28C-HI → S5700、S6730-H48X6C → S6700。导向后的系列仍必须在当前手册覆盖集合中；其它 SKU 不做前缀或相似度猜测。
~~~

因此，型号树中已保存且未被拒绝的产品族/SKU 映射可以服务于系列兼容判断；拓扑型号下拉框的“已发布”展示规则仍与之独立。只要解析出的系列存在于当前手册已抽取、已发布的系列集合，计划就给出当前枚举值 `exact`。该名称容易误读，其真实含义是“系列匹配”，并不是“SKU/版本精确匹配”。运行版本只写入审计字段，当前不作为阻断条件。

### 5.4 S5735/S5755 → S5700 的已验证路径

项目采用“系列级手册适配”策略：设备型号只要可由型号库的显式父子关系归属已覆盖系列，即可检索该系列的手册命令。当前本地华为样例型号库中，`S5735` 与 `S5755` 都是 `S5700` 的子节点；规划阶段和实际写入前的 `display version` 二次确认都会走同一条父链，不依赖把 `S5735` 或 `S5755` 写死为字符串规则。

本地 SQLite 走查及单元测试结果：

| 设备型号 | 解析来源 / 路径 | 结果 |
| --- | --- | --- |
| S5700-28C-HI | 华为已知系列前缀兜底 → S5700 | 可以通过系列门禁 |
| S5735 | 型号树：S5735 → S5700 | 可以通过 S5700 系列门禁 |
| S5755 | 型号树：S5755 → S5700 | 可以通过 S5700 系列门禁 |
| S5755-S48T4X-A | 型号别名 → S5755 → S5700（需在型号库存在该别名） | 可以通过 S5700 系列门禁 |
| S9999 | 无目录映射、无允许的兜底 | 保守阻断 |

这不是按型号名称的无限放宽。设备型号必须能精确命中目录或有限的华为系列兜底；映射被拒绝、父链环路、别名缺失，或同一字符串存在多条不同的已覆盖系列路径时，都不授权下发。后续工作是扩大并审查目录覆盖率，而不是增加宽松的字符串猜测：

1. 补充设备实际回显的别名，例如 `S5755-S48T4X-A`；当前本地样例库尚未含该别名。
2. 抽样复核自动抽取出的 parent_id，拒绝错误映射；被拒绝的节点或祖先不会授权。
3. 补充父链环路、跨品牌同名型号、歧义别名和更多厂商目录的回归测试。
4. 统一端口别名，并将 `exact` 重命名为 `series_matched` 或增加更准确的状态，降低 UI 误读。

## 6. 命令知识库与检索算法

### 6.1 Command 中保存什么

每条命令对象包含命令名、语法、视图、参数、前置条件、约束、示例与来源路径。例如：

~~~json
{
  "canonical_name": "port default vlan",
  "syntax": ["port default vlan vlan-id"],
  "views": ["GE interface view"],
  "parameters": [{"name": "vlan-id", "description": "...", "range": "..."}],
  "preconditions": ["..."],
  "constraints": ["..."],
  "examples": ["..."],
  "evidence": {"source_path": "...html", "toc_path": ["..."]}
}
~~~

结构化字段用于算法验证，全文正文用于搜索和人工追溯。

### 6.2 已接入的混合检索：命令名、FTS5 与 CPU Embedding

配置规划页命令搜索 API 和规划器检索节点共用 `hybrid_command_search`。算法先硬过滤手册 ID（及存在时的显式“不支持型号”关系），再合并三类候选：

~~~text
候选 A：命令名/功能的精确或包含匹配
候选 B：SQLite FTS5，对输入拆为安全词项，用 BM25 排序
候选 C：已建索引时，以 OpenAI 兼容 Embedding 生成查询向量，在本机 SQLite BLOB 上做 NumPy CPU 余弦 top-K
合并：A 的精确命令优先；B/C 的多路一致结果小幅加分；去重、重排并截断
~~~

FTS5 内容包括命令名、功能、页面标题、页面正文、命令格式、前置条件和约束。Embedding 文本包括命令名、功能、语法、条件、约束和最多 12,000 字页面正文。API 结果会标识 `exact_name`、`name`、`fts5`、`embedding_cpu` 等命中来源，方便人工检查检索为何命中。

未配置 Embedding、向量未建完、向量维度不一致或端点短暂失败时，语义分支返回空，检索继续使用命令名和 FTS5，不影响 VLAN 的确定性三证据门。若搜索带 model_id，当前只会过滤“已有明确适用性关系且标为不支持”的命令；没有明确关系时仍保留继承类命令供人工判断。

### 6.3 VLAN Access 的确定性证据检索

为让首个可写功能可复现，VLAN Access 固定要求三类证据：

~~~text
vlan batch
port link-type
port default vlan
~~~

规划器逐个查询当前手册。任一命令缺失，或 port default vlan 没有提取到以该命令开头的语法，都会清空候选命令并阻断计划。

取舍是覆盖面较小，但不会因为“搜到相关章节”就假装已经找到了正确命令格式。

### 6.4 资源边界与取舍

本实现没有引入 GPU、云向量库、Neo4j 或专有 SaaS：Embedding 由用户提供的 OpenAI 兼容接口生成，向量存 SQLite，比较在 CPU 完成。8,714 条样例命令的线性余弦扫描适合首期本地单用户场景；手册/命令规模明显增大后，应测量查询延迟，再考虑 SQLite 分块、缓存或外部向量索引，而不是提前引入依赖。

## 7. 拓扑 revision 与端口范围算法

### 7.1 为什么保存 revision

React Flow 画布可以不断编辑，但一次变更必须基于稳定输入。保存时，系统把节点、连线、端口映射、设备只读型号、SSH 地址/端口/用户名和 protected_ports 一起保存为 TopologyRevision JSON。

后续任务绑定 revision ID；执行时也从冻结 revision 读取设备、端口与 SSH 地址，而不是信任下发页面临时输入的范围。

### 7.2 保存时检查

拓扑保存会拒绝：

- 重复设备名称；
- 重复设备 IP；
- 连线引用不存在节点；
- 同一交换机端口出现在两条连线中。

端口没有映射时保存为 UNMAPPED；它不会进入规划端口集合。

### 7.3 端口集合算法

对每台交换机，遍历冻结拓扑的 links：

~~~text
link.source 是该交换机 → 取 source_port
link.target 是该交换机 → 取 target_port
空值和 UNMAPPED → 跳过
~~~

所得集合是唯一允许被渲染进接口命令的端口。系统不做 LLDP/CDP 自动发现，不扫描设备端口，也不从需求文字中猜测接口。

### 7.4 受保护端口与双表示端口比较

规划和执行都会比较映射端口与 protected_ports；命中即阻断。普通配置块也禁止出现 save、reboot、reset、format、delete、clear 等维护命令，保存只能在验证通过后由独立流程执行。

系统不改写用户在拓扑中填写的命令端口文本：`GE0/0/0` 会原样生成 `interface GE0/0/0` 与 `display port vlan GE0/0/0`，`GigabitEthernet0/0/0` 同理。这避免对不同设备的 CLI 方言做未经手册证实的强制改写。

同时，端口去重、`protected_ports` 比较、普通配置块预检和验证回显判断都使用比较键 `port_identity`：已知长短别名 `GE`/`GigabitEthernet`（以及 XGE/10GE、40GE、100GE 的已知长短形式）会归为同一物理接口；未知端口名称只做大小写和空白归一，不猜测。这意味着把 `GigabitEthernet0/0/2` 设为保护端口后，拓扑填写 `GE0/0/2` 也会被阻断。

## 8. 需求 → Intent → VLAN 命令

### 8.1 确定性基线 + 受约束 LLM Intent 精炼

第一道门是确定性 VLAN 正则：从需求提取 1 到 4094 的 VLAN ID。它定义可写范围；需求：

~~~text
创建 VLAN 10，并将交换机接入端口配置为 Access 加入 VLAN 10。
~~~

输出：

~~~json
{
  "source": "deterministic_baseline",
  "feature": "vlan_access",
  "vlan_ids": [10],
  "requires_llm_refinement": true
}
~~~

随后 LangGraph 的 `refine_with_llm` 节点按设置页配置调用 OpenAI 兼容 Chat Completions（固定 `httpx verify=False`）。模型只能返回下列 Pydantic Schema 的 JSON：

~~~json
{
  "action": "refine_intent",
  "feature": "vlan_access | unclassified",
  "vlan_ids": [10],
  "retrieval_terms": ["vlan batch", "port link-type"],
  "reason_summary": "从需求提取 VLAN 10，并检索对应命令。"
}
~~~

LLM 不得输出 CLI、接口名、设备目标、密码或工具调用；代码还会再次要求 `feature=vlan_access` 且 `vlan_ids` 与确定性基线完全相同。JSON 不合法、服务未配置/超时、或模型试图扩大 VLAN 范围时，记录 `llm.status=disabled/fallback/rejected` 并回退基线，而不是阻断已验证的 VLAN 演练。未知需求基线为 `unclassified`；当前没有受支持的确定性 renderer，仍会阻断写命令。

### 8.1.1 按节点自适应 thinking 策略（第一阶段已实现）

设置页的 `llm_thinking_mode` 有三个值：

| 值 | 行为 | 适用场景 |
| --- | --- | --- |
| `adaptive`（默认） | 仅推理型节点开启 | 日常运行，控制延迟和上下文消耗 |
| `always` | 所有实际 LLM 请求开启 | 端点质量较高、需要最大推理时使用 |
| `off` | 所有请求关闭 | 调试兼容性或低延迟场景 |

自适应白名单当前包含：`intent_refinement`、`retrieval_planning`、`evidence_review`、`command_plan`、`command_review`、`result_diagnosis`。目前已接通 `intent_refinement` 与首期 VLAN Access 的 `command_plan`；证据审查、命令审查和结果诊断在后续阶段接入。静态校验、拓扑端口保护、型号门禁、Netmiko 执行和验证后 `save` 都不调用 LLM。

开启时请求体为：

~~~json
{
  "chat_template_kwargs": {"enable_thinking": true},
  "thinking": {"type": "enabled"}
}
~~~

关闭时只发送 `{"chat_template_kwargs":{"enable_thinking":false}}`。若兼容端点以 400/404/422 明确拒绝 thinking 相关字段，客户端只重试一次关闭 thinking，并在 `intent.llm` 审计对象中记录 `thinking_requested`、`thinking_used`、`thinking_fallback` 和原因；认证失败、超时、模型不存在等错误不会被误判为参数兼容问题。

### 8.2 可执行计划的数学条件

~~~text
ExecutablePlan =
  IntentValid
  ∧ PortsMapped
  ∧ EvidenceComplete
  ∧ ProtectedPortSafe
  ∧ SeriesCompatible
~~~

任一条件不满足：

~~~text
commands = []
validation.status = blocked
task.status = blocked
~~~

### 8.3 当前命令渲染模板

对 VLAN 列表 V、首个 VLAN v、端口列表 P：

~~~text
system-view
vlan batch <V>
for each p in P:
    interface <p>
    port link-type access
    port default vlan <v>
    quit
return
~~~

例如 V=[10]、P=[GigabitEthernet0/0/1]：

~~~text
system-view
vlan batch 10
interface GigabitEthernet0/0/1
port link-type access
port default vlan 10
quit
return
~~~

多 VLAN 时，当前会创建所有 VLAN，但所有端口只进入列表第一个 VLAN。若要实现“不同端口属于不同 VLAN”，必须扩展 Intent，不应让自然语言隐含决定端口 VLAN 对应关系。

### 8.4 验证和回滚草案

计划同时生成：

~~~text
display vlan <所有 VLAN>
display port vlan <每个冻结拓扑端口>
~~~

VLAN Access 额外断言“每个 VLAN ID、每个目标端口名必须出现在验证输出”。它比“命令没报错即成功”严格，但仍是字符串断言；下一阶段应解析 VRP 输出并明确判断 VLAN、PVID、链路类型和成员关系。

系统会生成条件回滚草案，例如 undo port default vlan、undo vlan batch，但不自动执行。只有执行前快照证明 VLAN 原先不存在、端口 PVID 可以安全恢复时，才应由人工审核；当前没有回滚下发 UI。

## 9. LangGraph、LLM 与工具调用的真实位置

当前 LangGraph 是受控状态机：

~~~mermaid
stateDiagram-v2
  [*] --> validate_input
  validate_input --> refine_with_llm: 任务、设备和静态门禁齐全
  validate_input --> [*]: 输入缺失
  refine_with_llm --> retrieve_evidence: JSON 合法或安全回退
  retrieve_evidence --> generate_commands: 有手册证据
  retrieve_evidence --> [*]: 无证据
  retrieve_evidence --> plan_commands_with_llm: 已配置 LLM
  plan_commands_with_llm --> generate_commands
  generate_commands --> validate_commands: 有候选命令
  generate_commands --> [*]: 无候选命令
  validate_commands --> review_commands_with_llm: 已配置 LLM
  review_commands_with_llm --> prepare_human_review: approve 或降级
  review_commands_with_llm --> [*]: reject
  validate_commands --> prepare_human_review: 未配置 LLM
  prepare_human_review --> [*]
~~~

状态里包含任务 ID、设备 ID、Intent、证据、候选命令、校验错误和 next_action。

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| LangGraph 状态转移 | 已接通 | 规划服务实际调用 |
| LLM Intent 精炼 | 已接通 | 仅 Schema JSON；与确定性 VLAN 基线不一致即回退；支持按节点 thinking |
| LLM CommandPlan | 已接通（VLAN Access） | 选择手册命令、参数和拓扑端口引用；确定性编译器才生成 CLI |
| LLM 命令审查 | 已接通 | 只能 approve/reject 已编译命令，不能改命令或触发下发；未配置/失败时降级 |
| 证据检索 | 已接通 | 显式图节点调用混合检索适配器，模型不能直接查库 |
| VLAN Intent 和命令模板 | 已接通 | 正则 + 模板 |
| OpenAI 兼容 LLM 客户端 | 已接通 | 固定 httpx verify=False |
| thinking 端点兼容降级 | 已接通 | 可选字段被明确拒绝时，单次关闭 thinking 重试并审计 |
| LLM JSON Schema 校验 | 已接通 | `LlmIntentRefinement`，拒绝额外字段 |
| 原生 function calling | 未使用 | 符合项目约束 |
| ReAct 动态工具循环 | 未使用 | 当前不允许模型自主决定或循环调用工具 |
| Embedding 混合排序 | 已接通 | 有本地索引时加入 CPU 余弦候选，无索引自动降级 |

当前受控调用顺序是：

~~~text
确定性基线 Intent
→ LLM 输出严格 Intent JSON
→ Pydantic Schema + VLAN 范围一致性校验
→ LangGraph 白名单检索节点执行混合只读检索
→ 证据写回 state
→ LLM CommandPlan（若模型可用）或确定性降级模板
→ 确定性命令编译与静态验证
→ 人工审批
~~~

LLM 不应持有 SSH 密码，不应决定保护端口例外，也不应直接触发 Netmiko 写入。

### 9.1 从“意图精炼”演进到“拓扑 + 需求 → 每台设备命令”

最终链路不是让模型自由输出一段 CLI，而是让模型输出受 Schema 约束的 `CommandPlan`，再由确定性编译器生成实际命令：

~~~text
拓扑 revision + 需求
→ 设备身份/系列门禁
→ LLM 需求拆解与设备作用域（只能引用已有节点和连线）
→ LangGraph 显式混合检索
→ LLM 证据充分性判断（不足时请求补检索，最多 3 轮）
→ LLM CommandPlan（command_id、syntax_index、参数、拓扑端口引用、顺序、验证计划）
→ 确定性命令编译器（视图切换、参数转义、端口原文）
→ 静态安全校验
→ 独立 LLM 命令审查与最多 2 轮受限修复
→ 用户逐台确认
→ Netmiko 执行、确定性验证、自动 save
→ LLM 结果解释/只读补查建议
~~~

`CommandPlan` 的每条写操作必须绑定当前手册证据 ID；端口只能引用拓扑中的已连端口；VLAN、地址、设备和保护端口范围不能由模型新增。模型不能拿到 SSH 密码、不能直接调用 Netmiko、不能决定跳过验证或提前 `save`。当证据、参数或静态门任一失败时，计划清空为 blocked，并保留失败原因供用户修正。

首期 VLAN Access 的“端口作用域”进一步限定为：只允许交换机端点的对端明确标记为 `pc` 的拓扑连线。交换机—交换机、交换机—云、未标注或 `UNMAPPED` 连线不可以被解释为接入口。这个规则同时用于规划、CommandPlan 编译和执行前命令扫描；因此即便审批后有人将命令改为 `interface GE0/0/2`，只要该端口不是 PC 链路，也会在 Netmiko 建连前阻断。

该演进分为：

1. 第一阶段（已完成）：自适应 thinking 配置与兼容端点降级；LLM 受限精炼检索词，确定性 VLAN renderer 产出每台设备命令。
2. 第二阶段（已完成，首期 VLAN Access）：已引入 `CommandPlan` Schema 和 LangGraph `command_plan` 节点。模型只能从本轮混合检索回来的 `command_id` 中选择命令，并提交 `syntax_index`、参数与 `topology:port:<原始端口>` 引用；确定性编译器检查三种必需命令、语法索引、VLAN 参数、端口来源和 Access 类型后才输出 CLI。模型不可用时回退既有确定性 VLAN 模板。
3. 第三阶段（已完成首期审查）：已引入 `command_review` 节点。它独立审查已编译 CLI、CommandPlan、静态校验与手册证据；`reject` 会阻断计划且不会改写命令，模型不可用时降级为确定性结果。下一小步才是最多两轮的受限修复；每次修复都将增加 approval revision，不能绕过用户确认。
4. 第四阶段：扩展 Trunk、管理 VLAN、链路聚合、STP、ACL、三层接口等功能插件，每个插件均需独立手册证据门、编译器、验证器和回滚策略。

## 10. 审批 revision 算法

只有所有交换机都“系列兼容 + 静态验证 ready”时，任务才到 needs_review。审批某台设备要求：

~~~text
当前兼容状态为系列匹配
AND 用户提交 revision 等于当前 plan 的 approval_revision
AND validation.status == ready
~~~

审批成功后写入 approved_at。若后端接收 command_overrides：

1. approval_revision 加一；
2. 原条件回滚草案失效，改为 manual_review_required；
3. 必须重新审批新 revision。

这样可防止用户审阅的是 A、系统随后静默执行 B。当前前端还没有命令编辑入口，但服务端 revision 机制已经存在。

## 11. 单台执行、验证与自动保存算法

### 11.1 执行前预检

每次执行会新建 ExecutionRun，再检查：

1. 当前计划为系列匹配；
2. 当前计划已人工审批；
3. 静态验证状态为 ready；
4. 若不是第一台，前一台最近执行必须 completed；
5. 本次填写的 SSH host/port 与冻结拓扑一致；
6. 普通配置块没有维护类关键字；
7. 接口命令没有进入保护端口。

预检失败：

~~~text
status = preflight_blocked
不连接设备，不发送任何命令
~~~

### 11.2 现场身份二次确认

预检通过才连接设备，首先发送：

~~~text
display version
~~~

解析现场型号后，执行器以与规划阶段相同的 `resolve_series_for_model` 重新解析：

~~~text
现场回显型号 --精确 canonical/alias 匹配--> 型号库节点
             --沿 parent_id--> 已覆盖 series
             --与 plan.mapped_series 相等--> 允许写入
~~~

例如 `S5735 → S5700`、`S5755 → S5700` 会与审批的 `S5700` 匹配；`S5700-28C-HI` 可走有限的华为系列前缀兜底。现场型号无法解析、父链被拒绝/成环、解析到不同系列，或解析结果不等于 `plan.mapped_series` 时均立即终止，尚未发送配置。这样计划阶段和写入前的身份门禁不会因不同的字符串规则而产生偏差。

### 11.3 逐条配置和错误识别

每条配置以 Netmiko send_command_timing 单独发送；命令、回显、阶段、成功标记写到 execution_commands。回显中包含下列模式即判失败：

~~~text
error
fail / failed
unrecognized command
wrong parameter
incomplete command
too many parameters
~~~

一旦失败：

~~~text
status = command_failed
停止后续命令、验证和 save
不自动回滚
~~~

### 11.4 验证和 save

正向命令完成后：

1. 运行计划生成的 display 验证命令；
2. 记录每条输出；
3. 运行 VLAN Access 期望状态断言；
4. 任一验证失败 → validation_failed，绝不 save；
5. 全部通过 → 单独发起 save。

保存用交互式连接处理 VRP：

~~~text
出现 [Y/N] → 发送 y
出现默认配置文件名提示 → 发送空行
~~~

只有成功回显匹配且不含错误模式，才把 save.success 置为 true 并设置 completed。验证成功但保存回显不明确时仍为 failed，必须人工核实设备保存状态。

### 11.5 可选 PC SSH ping

只有交换机执行 completed（含 save 成功）才允许 PC SSH ping。目标必须是 IPv4，命令被限制为 Linux/Windows 固定 ping 形式；PC 密码不保存。没有明确 PC SSH 资产、系统和凭据时应该跳过，不要把 eNSP 控制台人工验证误记为自动验收。

## 12. 状态含义

| 任务状态 | 含义 | 是否可写设备 |
| --- | --- | --- |
| planning | 正在生成计划 | 否 |
| needs_review | 门禁通过，等待人工审批 | 否 |
| blocked | 型号、证据、端口或静态校验失败 | 否 |
| approved | 全部设备已审批 | 可按顺序逐台申请 |
| executing | 存在执行中的设备 | 受前序门禁限制 |
| completed | 当前任务成功 | 可选 PC ping |

| 执行状态 | 已连接 | 已写配置 | 已 save | 含义 |
| --- | ---: | ---: | ---: | --- |
| preflight_blocked | 否 | 否 | 否 | 本地预检拒绝 |
| command_failed | 是 | 部分可能已写 | 否 | 某条命令被设备拒绝 |
| validation_failed | 是 | 是 | 否 | 设备状态不符合预期 |
| completed | 是 | 是 | 是 | 验证和保存成功 |
| failed | 不确定 | 不确定 | 可能尝试 | 必须查看记录，特别确认保存状态 |

## 13. 关键数据对象

~~~mermaid
erDiagram
  MANUAL ||--o{ KNOWLEDGE_DOCUMENT : contains
  MANUAL ||--o{ COMMAND : provides
  MANUAL ||--o{ DEVICE_MODEL : seeds
  KNOWLEDGE_DOCUMENT ||--o{ COMMAND : source_page
  DEVICE_MODEL ||--o{ DEVICE_MODEL : parent_child
  DEVICE_MODEL ||--o{ MODEL_ALIAS : aliases
  DEVICE_MODEL ||--o{ MODEL_EVIDENCE : evidence
  COMMAND ||--o{ COMMAND_APPLICABILITY : applies_to
  COMMAND ||--o{ COMMAND_EMBEDDING : optional_vector
  TOPOLOGY ||--o{ TOPOLOGY_REVISION : snapshots
  TOPOLOGY_REVISION ||--o{ CONFIG_TASK : frozen_for
  CONFIG_TASK ||--o{ DEVICE_PLAN : per_device
  DEVICE_PLAN ||--o{ EXECUTION_RUN : attempts
  EXECUTION_RUN ||--o{ EXECUTION_COMMAND : command_audit
  EXECUTION_RUN ||--o{ PC_PING_RUN : optional_acceptance
~~~

可以理解为两条独立且可追溯的链：

~~~text
证据链：手册 → 页面 → 命令 → 型号/适用性 → 设备计划
审计链：拓扑 revision → 任务 → 审批 revision → 执行 → 每条回显/save/ping
~~~

历史执行能追溯到当时绑定的手册、命令证据和拓扑快照。

## 14. 华为 VLAN 10 示例：逐步输入输出

### 输入

~~~text
手册：S1700, S5700, S6700 V600R025C00 命令参考.chm
设备：SW1，设备回显 S5700-28C-HI
拓扑：GigabitEthernet0/0/1 ↔ PC1 Ethernet0/0/1
保护端口：GigabitEthernet0/0/2
需求：创建 VLAN 10，并将交换机接入端口配置为 Access 加入 VLAN 10。
~~~

样例手册当前抽取统计为 8,914 页、8,714 条命令、180 个型号候选、0 个失败页。

### 中间输出

~~~text
Intent：
  feature = vlan_access
  vlan_ids = [10]

型号：
  S5700-28C-HI → S5700
  S5700 在手册覆盖系列中 → 通过

拓扑：
  permitted_ports = [GigabitEthernet0/0/1]
  protected_ports = [GigabitEthernet0/0/2]
  两集合无交集 → 通过

证据：
  vlan batch、port link-type、port default vlan 均存在 → 通过
~~~

### 输出计划

~~~text
system-view
vlan batch 10
interface GigabitEthernet0/0/1
port link-type access
port default vlan 10
quit
return

验证：
display vlan 10
display port vlan GigabitEthernet0/0/1
~~~

此时只是 needs_review。用户审批后，执行器再次 `display version`，以同一型号树解析器确认现场型号仍归属审批的 S5700 系列，再逐条下发。只有 VLAN 10 和目标端口同时出现在验证回显中，才会调用 save。

## 15. 质量门与待修复风险

### 15.1 已实现的质量门

| 风险 | 当前控制 |
| --- | --- |
| 重复手册 | SHA-256 去重 |
| CHM 解包为空 | 没有 HTML 即失败 |
| 个别页面坏 | 单页容错、断点重试 |
| 手册证据不足 | 不生成写命令 |
| 端口没映射 | 不进入计划 |
| 保护端口 | 规划和执行双重检查 |
| 型号不在系列 | 计划阻断 + 执行前二次确认 |
| 审批内容变化 | approval_revision |
| 批量扩大影响 | 一次执行一台，前序门禁 |
| 命令失败后继续 | 第一条失败即停止 |
| 验证失败仍保存 | 验证失败绝不 save |
| SSH 密码留存 | 密码不入库、不写日志 |
| PC 变通远程 shell | 只允许 IPv4 ping 白名单 |

### 15.2 未关闭的风险

| 缺口 | 影响 | 优先级 |
| --- | --- | --- |
| 型号库的别名/父级覆盖率尚未量化 | 未收录的实际 SKU 或错误自动映射会保守阻断，需要人工修正并扩大样本评测 | P1 |
| exact 状态名不准确 | 容易误读为 SKU/版本精确匹配 | P1 |
| PDF/文本/一般 HTML 没有命令结构抽取 | 多格式只能导入，不能产出可执行计划 | P1 |
| Embedding 全量线性扫描 | 当前样例规模可接受，但更大手册库的 CPU 延迟和内存仍需压测 | P1 |
| LLM 首期只能精炼 VLAN Intent | 对未知功能仍会阻断，不能以 LLM 文本替代厂商插件和确定性 renderer | P1 |
| 验证仍是字符串断言 | 可能受显示格式影响 | P1 |
| 前快照与回滚未闭环 | 不能安全自动回滚 | P1 |
| 下发页面需手输 ID | GUI 流程尚未完全闭环 | P2 |

## 16. 下一阶段算法建议

1. 型号库覆盖率：从真实 `display version` 样本补齐别名，抽样审查自动 parent_id，建立跨品牌、歧义和环路回归集。
2. 检索重排压测：按不同手册规模测量 CPU 向量扫描；叠加命令语法完整度、视图与显式适用性重排。
3. LLM Intent 扩展：每新增厂商功能先定义严格 Intent schema、允许检索词和“不得扩大范围”的字段一致性规则。
4. 功能插件：每厂商/功能定义命令 renderer、静态校验、前快照解析、后验证解析、回滚判定。
5. GUI 闭环：计划卡片直接提供审核后下发、完整 ID、命令编辑复审、执行历史、回滚审批和导出。

## 17. 可复核检查

以下命令只做本地测试、静态检查或本机 API 查询，不连接设备：

~~~powershell
cd D:\network-automation

uv run pytest apps/api/tests -q
uv run ruff check apps/api
pnpm --filter network-automation-web run build

Invoke-RestMethod http://127.0.0.1:8000/api/manuals | ConvertTo-Json -Depth 4

uv run python -c "import sys; sys.path.insert(0, 'apps/api'); from app.db import SessionLocal; from app.model_resolution import resolve_series_for_model; s=SessionLocal(); print({m:resolve_series_for_model(s, model_name=m, brand='Huawei', covered_series={'S5700'}) for m in ['S5700-28C-HI','S5735','S5755','S9999']}); s.close()"

uv run python -c "import sys; sys.path.insert(0, 'apps/api'); from app.ports import port_identity; print(port_identity('GE0/0/2'), port_identity('GigabitEthernet0/0/2'))"
~~~

当前走查已确认：后端测试、Ruff 和前端生产构建通过；本地型号库中 `S5700-28C-HI → S5700`（有限子系列兜底）、`S5735 → S5700`、`S5755 → S5700`（型号树父链）均能解析，未知的 `S9999` 返回空并被保守阻断。回归测试还覆盖 `S5755-S48T4X-A` 别名路径、rejected 映射拒绝、`S6730 → S6700` 的手册覆盖门禁、GE/GigabitEthernet 保护端口等价比较，以及 LangGraph 中 LLM → 检索 → 渲染的受控状态转移。

## 18. 结论

当前版本已经形成一条本地、逐台、可审计、手册证据驱动的受限变更链。价值不在于覆盖全部厂商命令，而在于把手册证据、拓扑边界、设备身份、人工审批、设备验证和保存回显放进同一条可追溯链。

现阶段应定位为“华为 CHM 驱动的 VLAN Access 实验工作台”。它已经完成 S5700 系列及已映射的子型号（包括 `S5735/S5755 → S5700`）、保留原始端口 CLI 文本而安全比较 GE/GigabitEthernet、混合检索、受约束 LLM Intent 精炼、单个授权端口、VLAN 10、验证后 save 的闭环；要扩大到更多 SKU、生产网络、多格式手册、自由文本需求和可回滚变更前，必须继续关闭第 15.2 节的 P1 风险。
